doctype html
html
    head
        title Tareas
    body
        h1=task.description
        p= task.createdAt
        form(action=`/tasks/${task.id}?_method=DELETE` method="POST")
            input(type="submit" value="Eliminae")
